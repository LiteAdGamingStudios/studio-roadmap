in CMD command

# Minimal (for simple GUI)
pip install kivy

# Recommended (core + examples)
pip install kivy[base] kivy_examples

# Full (includes media, audio, camera)
pip install kivy[full]



To Run any python file

python lottery_predictor.py
